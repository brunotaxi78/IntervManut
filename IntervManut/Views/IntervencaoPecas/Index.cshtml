@model IEnumerable<IntervManut.Models.IntervencaoPeca>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Intervencao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Peca)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Intervencao.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Peca.Descricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntervencaoPecaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntervencaoPecaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntervencaoPecaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
