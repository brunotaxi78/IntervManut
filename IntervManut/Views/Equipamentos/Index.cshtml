@model IEnumerable<IntervManut.Models.Equipamento>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a class="btn btn-outline-primary" asp-action="Create">Adicionar</a>
</p>
<table class="table table-hover">
    <thead>
        <tr class="bg-secondary">
            <th>
                #
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodEquipamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodAtivo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LinhaProd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EquipamentoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodEquipamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodAtivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LinhaProd.CodLinha)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EquipamentoId"><i class="fa fa-edit"></i></a> |
                <a asp-action="Details" asp-route-id="@item.EquipamentoId"><i class="fa fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.EquipamentoId"><i class="fa fa-trash"></i></a>
            </td>
        </tr>
        }
    </tbody>
</table>
