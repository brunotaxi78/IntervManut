@model IEnumerable<IntervManut.Models.Tecnico>

@{
    ViewData["Title"] = "Index";
}

<p>
    <a class="btn btn-outline-primary" asp-action="Create">Adicionar</a>
</p>
<table class="table table-hover">
    <thead>
        <tr class="bg-secondary">
            <th>
                #
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Empresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoTec)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TecnicoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Empresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoTec.TipoTecId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TecnicoId"><i class="fa fa-edit"></i></a> |
                <a asp-action="Details" asp-route-id="@item.TecnicoId"><i class="fa fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.TecnicoId"><i class="fa fa-trash"></i></a>
            </td>
        </tr>
        }
    </tbody>
</table>
